import React from "react";
import classnames from 'classnames';
 
export function Meeting(): React.JSX.Element {
  return (
    <main className={classnames('omrs-main-content')}>
      <h3> Meeting module </h3>
    </main>
  );
}